# 当前开发明细

日期：2026-07-16

## 一、路线

主开发工程：

```text
代码/planning-platform
```

`ruoyi-vue-pro` 和 `JeecgBoot` 仅作为参考项目，不再作为主工程二开。

## 二、后端已完成

目录：

```text
代码/planning-platform/backend
```

已完成：

- Spring Boot 3.3.7 + Java 17。
- MySQL 自动建库建表，Redis 会话配置。
- Spring Security + JWT access token / refresh token。
- `/auth/login`、`/auth/me`、`/auth/logout`、`/auth/refresh`、`/auth/change-password`。
- 初始化超级管理员 `admin / Admin@123456`。
- 基础表：部门、角色、权限、用户、用户角色、角色权限、审计日志。
- 系统管理端后端已完成：管理工作台、员工注册、员工管理、部门/项目组、角色、权限、工作日规则和审计日志。
- 员工由管理员直接注册，不发送邀请或开户通知；系统生成全局唯一 8 位数字账号，默认密码 `123456`，首次登录必须修改密码后才能访问业务页面。
- 刷新登录会话时重新读取数据库账号状态、首次改密标志、角色和权限；账号禁用、密码重置或角色停用后不会继续沿用刷新令牌中的旧权限快照。
- 员工注册页只提交姓名、手机号、主部门/小组和直属负责人；员工编号自动生成，默认分配 `EMPLOYEE` 角色。
- 员工管理支持编辑、启停、密码重置、Excel 导入模板、批量导入和 CSV 导出；导入模板附带当前可选组织与直属负责人 ID。
- 组织树支持部门、小组和项目组；角色支持内置保护和数据范围；权限按树保存并允许角色清空为零权限。
- 工作日规则采用历史版本保留方式，已接入员工工作台日历、应填未填统计和日计划编辑提示，默认周一至周五强制填报、周末非强制，可由显式规则覆盖。
- 系统管理关键写操作、导入导出和模板下载均写入审计日志；审计页只读并支持筛选和 CSV 导出。
- 业务表：月计划、日计划、成果、成果附件。
- `/planning/month-plans`、`/planning/day-plans`、`/planning/results` 保留只读列表能力，员工、直属领导和部门负责人写操作统一走各角色流程接口，避免绕过审批、待办和强认证。
- 员工端支持月计划和日计划草稿、提交成果、申诉、计划调整及个人绩效依据导出。
- 直属领导端支持日计划点评、风险标记、成果建议、计划调整处理和下属台账导出。
- 部门负责人端支持月计划审批、成果最终确认、申诉处理、配置维护、部门台账和异步导出任务。
- 日计划风险标记后自动关闭直属领导待办并生成部门负责人补审待办，部门负责人可查看完整上下文、复核通过或退回员工补充。
- 历史日计划风险待办会在启动时自动改派至部门负责人，并修正场景名称和处理路由。
- 员工被退回后重新提交日计划时，会显式清空上一轮领导意见、部门补审意见、处理人和处理时间，重新进入直属领导初审。
- 员工被驳回后重新提交月计划时，会清空上一轮审批意见、审批人和审批时间，并提升版本号；待确认或已确认成果禁止针对同一计划事项重复提交。
- 员工工作台的成果数量和平均完成比例按当前月份对应月计划统计，成果状态按各事项最新版本汇总，不再受历史版本干扰。
- 员工工作台和日计划编辑页的归属组织来自 `sys_dept`，申诉待办数按数据库未完成申诉实时统计，不再显示静态组织和固定数量。
- 工作台统计：待审日计划数、逾期未审数、本月成果数、计划执行闭环率。
- 员工端、直属领导端、部门负责人端已共用 MySQL 业务数据，不再使用性能模块内存假数据。
- 新增日计划点评、成果建议、计划调整、通知待办、交付模板、验收标准、参考分规则和导出任务持久化表。
- PDF、Word、Zip 资料包可生成和下载，支持成果证据打包与 SHA-256 完整性校验。
- 部门导出任务支持 `PENDING -> PROCESSING -> SUCCESS/FAILED`、篡改识别、重试、过期清理和超时恢复。
- 员工申诉资料包可按数据库最新记录生成，包含申诉、关联计划、成果、确认意见与证据附件。
- 直属领导下属台账支持团队导出和单个下属资料包下载，个人导出按员工数据范围校验。
- 新增 `/leader/org-tree` 和 `/department/org-tree`，分别返回当前账号真实授权组织；列表筛选和导出均校验组织范围，越权请求返回 `403`。
- 月计划审批和成果最终确认增加终态保护，已完成记录不能再次审批、驳回或覆盖。
- 同一员工同月只允许一条有效月计划，同日只允许一条有效日计划；新建、编辑变更周期和不存在 ID 均有明确校验。
- 部门总览的月度、季度、年度统计已统一作用于月计划、成果和逾期日计划。
- 参考分规则按数据库保存的完成比例、逾期次数、驳回次数、证据完整和评审通过因素实际计算，并校验启用权重总和。
- 员工流程接口只允许数据所有人写入；通用计划列表和统计按当前账号授权人员范围查询，管理角色不再默认越过组织边界。
- 领导工作台的待点评和待建议共用同一日期或月度周期；已处理的暂停撤销申请再次提交返回 `409`。
- 员工、领导和部门周期月份统一校验为真实 `yyyy-MM`，部门审批月份限定为 1 到 12，非法输入统一返回 `422`。
- 月计划和日计划新建按员工行串行化；成果提交、领导点评/建议、部门审批/确认/补审、待办和申诉处理均使用 MySQL `FOR UPDATE` 锁定当前业务记录，防止并发请求覆盖终态。
- 员工日计划只返回并允许关联已审批通过的月计划事项，即使绕过前端传入草稿或驳回计划事项也返回 `422`。
- 导出任务完整性校验和失败重试锁定任务行，重试的异步生成仅在数据库事务提交后启动，避免并发重试删除正在生成的文件。
- 导出文件按业务类型读取真实数据：日计划点评导出日计划，暂停撤销导出调整记录，计划审批导出月计划，成果清单导出成果；组织导出使用所选组织或授权下属快照，不再默认扩大到导出人的整个部门。
- 审计服务统一把业务明细规范为合法 JSON，避免 MySQL JSON 字段写入失败。
- 成果附件校验 PDF、Word、DOCX、Zip 实际文件结构，下载路径限制在配置目录内，事务回滚时清理已落盘文件。
- 参数校验、请求方法错误、资源不存在和超大附件分别返回标准 `422/405/404/413`，未知异常不向客户端泄露内部信息。
- 部门配置维护接口会同时校验原记录和目标组织的数据范围，员工账号不能绕过页面调用启停或试算接口；验收标准保存后不能迁移到其他交付物模板。
- 交付物模板的证据类型和适用场景统一保存为数据库编码，支持文档、表格、图片和附件，旧演示数据会自动规范为 `MONTH_PLAN,RESULT`。
- 部门台账使用部门负责人授权范围查询，不再复用直属领导下属范围；员工月计划提交会校验预计工时和完成比例，员工请求字段长度与数据库字段保持一致。

本轮验证：

```bash
mvn.cmd test
```

当前共有 105 项后端测试，全部通过。

## 三、PC 管理端已完成

目录：

```text
代码/planning-platform/web-admin
```

已完成：

- 登录页和路由登录拦截。
- Axios 自动携带 `Authorization: Bearer <token>`。
- 工作台统计页接入 `/planning/stats`。
- 员工端：工作台、月计划、日计划、成果提交与记录、绩效依据和申诉。
- 直属领导端：领导工作台、日计划点评、成果建议、暂停撤销和下属台账。
- 部门负责人端：部门总览、月计划审批、成果最终确认、通知待办、交付物模板、验收标准、参考分规则、部门台账和导出任务。
- 系统管理端：管理工作台、员工注册、员工管理、部门/项目组、角色管理、权限管理、工作日规则和审计日志。
- `SUPER_ADMIN` 和 `SYS_ADMIN` 登录后默认进入系统管理工作台，仍可从“业务入口”切换其他授权工作空间。
- 首次登录强制改密页已接入路由守卫；员工使用默认密码登录时只能修改密码、刷新会话或退出，改密后重新登录进入业务页面。
- 员工管理导入提供实时生成的 Excel 模板；员工注册成功页展示并可复制账号、默认密码和员工编号，不增加邀请或通知步骤。
- 工作日规则已在员工日历中标识非强制日期、特殊排班和应填未填日期，日计划编辑页同步显示当日规则说明。
- 员工端已移除员工自行审批日计划、确认成果等越权操作。
- 员工端可查看直属领导点评、部门审批意见和成果确认时间轴。
- 员工月计划自动保存已抑制详情回填触发的重复保存，并防止旧保存响应覆盖请求期间的新输入；已有明细按原 ID 更新，新增项插入、移除项确认后软删除，避免日计划关联失效和废弃明细持续增长。
- 员工日计划页已移除固定文本拼接的伪 AI 草案，点评时限与后端 1 天规则保持一致；快速切换日期不会再被旧请求串页，保存和提交期间锁定表单。
- 员工工作台只对已审批且待提交或被退回的月计划开放成果入口；成果提交附件类型读取后端配置，申诉和绩效依据页面会刷新真实选项并防止并发请求覆盖。
- 前端 API 失败时不再回退到内存假数据。
- 部门导出任务支持周期选择、自动刷新、完整性复核、失败重试和 Blob 文件下载。
- 员工申诉页已接入真实裁决资料包下载，并落实 3 天申诉期和重复申诉限制。
- 直属领导下属台账导出后可直接下载，单人资料包不会混入其他下属数据。
- 员工绩效依据支持日、周、月、季、年周期，并汇总月计划、日计划、成果和申诉数据库记录。
- 部门通知待办支持对象级跳转、申诉受理与处理、资料包下载以及导出完成通知。
- 全角色统一消息中心已接入：顶部消息入口显示本人真实未读数，相关侧边栏菜单同步显示未读数字；支持查看全部本人消息、按类型筛选、单条已读、全部已读和业务对象跳转。
- 月/周/额外任务审批、日计划点评与风险补审、成果终审、计划调整、申诉、裁决、工作日规则及系统配置风险均已接入消息通知；员工注册仍不发送邀请或开户通知。
- 部门通知待办内置日计划补审抽屉，支持逾期提示、必要字段阻断、复核通过和退回补充；结果会同步到员工日计划详情与记录页。
- 直属领导日计划列表对已点评、风险已复核和需补充记录禁用重复操作，只允许待点评记录进入批量处理。
- 直属领导五个页面和部门负责人六个组织筛选页面均改为数据库授权组织树，不再写死产品中心、交付中心及固定组织 ID。
- 领导端日计划、成果建议、暂停撤销和下属台账导出可直接下载生成文件，并展示 SHA-256 校验值。
- 部门月计划审批只允许待审批记录进入批量选择和审批操作；成果确认补齐已驳回筛选、终态颜色和不可确认统计口径。
- 参考分试算移除张三、李四、王五等固定姓名，试算对象改为可选标识；规则组织来自数据库授权范围。
- 员工工作台只把已通过月计划的待提交或已退回成果计入待办；成果提交页会阻止同一计划事项重复提交待确认或已确认版本。
- 领导工作台默认按月查看，日期作为可选的精确筛选；切换月份会清除日期条件，选择日期会同步所在月份。
- 员工工作台、月计划列表和新建月计划改用本地时区计算当前月，避免月初凌晨因 UTC 转换落到上一个月。
- 审计日志按环境配置执行 90 天保留清理，系统管理端已提供只读查询、详情和导出页面。
- 日计划和成果记录默认查询当前月份，支持日期校验、重置和分页。
- Element Plus 改为按需构建，最大入口脚本由约 1.10 MB 降至约 253 KB。
- 开发 Compose 的 MySQL 和 Redis 仅绑定本机回环地址，端口与 MySQL 密码可通过环境变量覆盖；生产环境示例不再使用默认数据库密码和相对文件目录。
- JWT 密钥和令牌有效期增加启动阶段配置校验，弱密钥不会延迟到登录时才暴露。
- 交付物模板和验收标准启停增加确认、防重复操作和引用提示；参考分规则支持保存后启用，并可按完成比例、逾期、驳回、证据完整和评审结果进行真实试算。
- 员工月计划明细输入增加与后端一致的字段长度限制，异常工时和完成比例会在接口层返回明确校验错误，不再落到数据库异常。
- Vite 开发服务器默认端口固定为 `5180`，可通过 `VITE_PORT` 覆盖，端口占用时仍由 Vite 自动选择可用端口。

验证通过：

```bash
npm run build
```

运行时验证：

- 本轮系统管理验收前端：`http://127.0.0.1:5181`，HTTP `200`；后端：`http://127.0.0.1:48091/api`，HTTP `200`。
- 因本机默认端口已有旧进程，验收 MySQL 使用 `127.0.0.1:33306`，Redis 复用本机 `6379`；未停止或覆盖用户原有进程。
- `employee`、`leader`、`dept.owner` 三类演示账号登录和核心查询接口返回成功。
- 员工 2026-07 工作台读取 1 条月计划和 2 条日计划；领导代访员工详情返回 `403`，非法月份返回 `422`。
- 领导月度工作台返回 4 项指标和 3 条日期状态；已处理调整申请重复操作返回 `409`。
- 部门季度总览返回 4 项指标和 2 条组织汇总，数据库参考分规则试算结果为 `89.00`，命中 5 个因素。
- 部门总览、成果确认和月计划审批的非法月份请求均返回 `422`。
- 真实 MySQL 运行时下，同月计划、同日计划、已处理暂停撤销、已完成月计划审批和已终审成果的重复请求均返回 `409`。
- 领导 PDF 导出和部门异步 Word 导出均完成生成、下载及 64 位 SHA-256 校验；越权组织导出返回 HTTP `403`。
- 系统管理真实流程已通过：管理员直接注册、8 位数字账号、默认密码、首次访问 `403`、修改密码后登录、员工列表、组织/角色/权限读取、工作日规则影响员工日历、Excel 模板、员工 CSV、审计 CSV 和审计查询。
- 停用验收员工后，其刷新令牌返回 `403`；验收创建的临时员工和工作日规则已软删除，未保留在有效业务数据中。
- 使用本机 Edge 自动检查 8 个系统管理路由，1440 像素宽度下无页面级横向溢出；权限树限制为面板内滚动，避免页面被全部权限节点拉到 4000 像素以上。
- 本轮增量代码已通过 105 项后端测试和前端生产构建，截图级视觉验收完成。

## 四、uniapp 已完成

目录：

```text
代码/planning-platform/mobile-uniapp
```

已完成：

- 登录页，token 本地存储。
- 首页读取 `/auth/me`。
- 我的日计划：列表、新增草稿、提交审批。
- 我的成果：列表、新增草稿、提交确认；支持临时成果原因。
- 请求封装自动携带 `Authorization`。
- 修复 `pages.json`，H5 和微信小程序构建可用。

验证通过：

```bash
npm run build:h5
npm run build:mp-weixin
```

## 五、下一批建议

1. 数据范围：继续核对部门负责人下级范围和项目经理本组范围的边界用例。
2. 附件：阿里云 OSS 上传、图片/PDF 预览、Word/Excel 下载、PDF 水印。
3. 审计：继续补齐非系统管理模块的少量边缘动作留痕。
4. 业务体验：计划选择器替代手输计划 ID，增加待办审批入口。
5. 第二阶段 AI：辅助生成、辅助分析、辅助总结，不做自动审批。
